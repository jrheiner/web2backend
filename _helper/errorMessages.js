/**
 * Collection of shared error messages
 * @module error-messages
 */
const invalidJson = 'Form fields do not match requirements!';
const invalidLogin = 'Wrong username or password!';
const invalidPrivileges = 'Only the author can update this entity!';

module.exports = {
  invalidJson,
  invalidLogin,
  invalidPrivileges,
};
